{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "Blockchain simple implementation for learning purpose",
  "main": "main.js",
  "scripts": {
    "start": "npm run build && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./dist && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/crypto-js": "^4.0.2",
    "crypto-js": "^4.1.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.10",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  }
}
